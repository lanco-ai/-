let signedSteps=[],signedFingerprint='',signStep=0,policyType='home';
let consultItems=[],consultNext=null,consultDetail=null,consultMessages=[],consultMessageNext=null;
const CONSULT_CATEGORIES=['生长发育','入园准备','营养辅食','心理健康'];
function clearJourneySignatures(){signedSteps=[];signedFingerprint='';signStep=0;}
function clearConsultationState(){consultItems=[];consultDetail=null;consultMessages=[];consultNext=consultMessageNext=null;}
function serviceTypeLabel(t){return {home:'入户服务',center:'入托或到店'}[t]||'历史服务（类型未登记）';}
function serviceTypeSelect(id,value){return `<div class="field"><label for="${id}">服务类型 *</label><select id="${id}" name="${id}" required><option value="">请选择服务类型</option>${['home','center'].map(t=>`<option value="${t}" ${value===t?'selected':''}>${serviceTypeLabel(t)}</option>`).join('')}</select><div id="error-${id}" class="error"></div></div>`;}
function careIllustration(){return `<svg class="care-illustration" viewBox="0 0 300 230" aria-hidden="true"><ellipse cx="150" cy="199" rx="125" ry="18" fill="#dbe5cf"/><path d="M22 152Q4 67 67 43T162 28Q251 10 277 100T241 193H57Z" fill="#fff8e9"/><path d="M39 180V132M39 156Q12 135 20 119Q42 121 39 156M39 143Q63 115 73 130Q66 147 39 151" stroke="#7f9a6d" stroke-width="6" fill="#b9cb9f"/><path d="M94 181Q72 127 103 104L132 98Q156 113 151 181" fill="#689978"/><circle cx="120" cy="75" r="28" fill="#ebbc96"/><path d="M91 78Q81 31 120 40Q154 35 150 76L136 56Q116 75 91 78" fill="#555848"/><path d="M210 181Q235 132 207 108L175 98Q155 120 160 181" fill="#dbaa79"/><circle cx="188" cy="77" r="27" fill="#f1c8a5"/><path d="M160 74Q150 43 179 42Q209 26 217 61L207 81L199 60Q180 69 160 74" fill="#555848"/><path d="M130 182Q121 137 151 127Q177 135 172 182" fill="#f0d8a7"/><circle cx="151" cy="119" r="21" fill="#f3c8a6"/><path d="M141 100Q147 89 156 101" fill="none" stroke="#705a44" stroke-width="5" stroke-linecap="round"/><path d="M108 124Q111 163 146 169M198 128Q193 166 163 171" fill="none" stroke="#ebbc96" stroke-width="13" stroke-linecap="round"/><path d="M119 79h1m67 1h1m-44 40h1m13 0h1" stroke="#555848" stroke-width="4" stroke-linecap="round"/><path d="M147 127q5 5 10 0" stroke="#b87561" stroke-width="2" fill="none"/><path d="M244 53c-13-15-27 9 0 24c27-15 13-39 0-24" fill="#e6aa85"/></svg>`;}
function service(){return head('每一份托付，都认真照顾','预约服务、查看在园日常，或独立完成晨检确认。')+`<div class="care-banner"><span class="tag">近邻陪伴 · 0–3 岁</span><h2>从家门口出发，<br>一起照顾宝宝的小小世界。</h2><p>一份预约，一段有温度的陪伴。</p>${careIllustration()}</div><div class="grid3 journey-cards">${[['calendar','预约服务','入户指导或到店托育，选择适合家庭的服务。',staff()?'staff':'booking'],['heart','在园动态','照片、视频和老师记录，留住成长的小瞬间。','growth'],['smile','晨检记录','认真阅读每日检查，单独签字确认。',staff()?'staff/morning':'morning']].map(([i,t,d,r])=>`<section class="card feature"><span class="iconbox">${icon(i)}</span><h2>${t}</h2><p>${d}</p>${btn('进入'+t+' →',r)}</section>`).join('')}</div><section class="card institution-section"><h2>我的预约</h2>${appointmentList(state.appointments.slice(0,3))}${btn('查看全部预约',staff()?'staff':'personal/appointments',true)}</section><section class="card institution-section"><h2>了解普惠托育</h2><p>让科学照护走进社区，也走进日常。</p><button class="textbtn" data-action="service-info">阅读服务介绍 →</button></section>`;}
function academy(category='games'){
 const categories=[['games','居家游戏'],['safety','应急处理'],['manual','育儿手册'],['care','家庭照护提示卡'],['training','居家任务训练卡'],['articles','科普文章']];
 const tab=categories.some(([k])=>k===category)?category:'games';
 const nav=`<nav class="content-tabs" aria-label="育儿学堂分类">${categories.map(([k,t])=>`<a href="#/knowledge/${k}" ${tab===k?'aria-current="page"':''}>${t}</a>`).join('')}</nav>`;
 let content='';if(tab==='games')content=knowledge('games').replace(/<nav class="content-tabs"[\s\S]*?<\/nav>/,'');
 else if(tab==='articles')content=articleListing();
 else{const articleIds=tab==='safety'?[0,1,2]:[],resourceIds={safety:[3],manual:[0],care:[1,4],training:[2,5]}[tab];
  content=`<div class="grid3">${articleIds.map(i=>{const a=ARTICLES[i];return `<article class="card article-card">${cover(a,i)}<div class="article-content"><h3>${a.title}</h3><p>${a.desc}</p><a class="textbtn" href="#/article/${i}">阅读科普 →</a></div></article>`;}).join('')}${resourceIds.map(i=>{const r=RESOURCES[i];return `<article class="card article-card"><img class="game-cover" src="assets/resource-${i}-0.png" alt="${esc(r.title)}"><div class="article-content"><span class="tag">在线工具资料</span><h3>${r.title}</h3><p>${r.desc}</p><a class="textbtn" href="#/resource/${i}/0">在线阅读与收藏 →</a></div></article>`;}).join('')}</div>`;}
 content=content.replace(/^<div class="pagehead">[\s\S]*?<\/h1>(?:<p>[\s\S]*?<\/p>)?<\/div>/,'');
 return head('育儿学堂','把科学方法变成温柔的日常，一起慢慢学。')+nav+content;
}
function bookingPolicy(){return state.policies?.[state.draft.serviceType];}
function agreements(){
 const p=bookingPolicy();if(!state.draft.slotId||!p)return head('请先选择服务并填写预约')+btn('返回预约','booking');
 const fingerprint=JSON.stringify({draft:state.draft,policyId:p.id});if(signedFingerprint!==fingerprint){clearJourneySignatures();signedFingerprint=fingerprint;signData='';hasSignature=false;agreed=false;}
 const labels=['填写预约',p.documents[0].title,p.documents[1].title,'信息核对'];
 const stepper=`<div class="steps journey-steps">${labels.map((t,i)=>`<span class="step ${i<=signStep+1?'current':''}"><b>${i+1}</b>${esc(t)}</span>`).join('')}</div>`;
 if(signStep===2)return head('最后核对，一起安心出发','两份协议已分别确认，请核对健康资料。')+stepper+`<section class="card">${agreementBody(2,selectedDraft(),p.documents,p.organization)}<p class="note">两份协议均已手写签名，提交后保存本次文本和确认时间。</p><label class="check"><input type="checkbox" id="final-verified">我确认本次预约及健康登记信息准确。</label><div class="form-actions"><button class="btn secondary" data-journey-back>上一步</button>${btn('修改预约资料','booking',true)}<button class="btn" data-journey-submit disabled>提交预约</button></div></section>`;
 const saved=signedSteps[signStep];signData=saved?.signature||'';hasSignature=!!signData;agreed=!!saved?.agreed;
 return head('逐份阅读，安心签署',serviceTypeLabel(state.draft.serviceType)+' · '+(signStep+1)+' / 2')+stepper+`<section class="card"><div class="note">${esc(p.organization)} · 正式协议版本 ${fmt(p.created_at)}</div>${agreementBody(signStep,selectedDraft(),p.documents,p.organization)}<div class="summary">${summary(selectedDraft())}</div><label class="check"><input id="agree" type="checkbox" ${agreed?'checked':''}>我已阅读本份协议，核对以上资料并同意。</label><div class="between"><h3>本份协议家长手写签名</h3><button class="textbtn" data-journey-clear>清空签名</button></div><p class="tiny">手机／平板可用手指签名，电脑可用鼠标签名。请分别签署每份协议。</p><div class="signature"><canvas id="signature" aria-label="本份协议家长签名板"></canvas><span class="hint" ${hasSignature?'hidden':''}>请在此签名</span></div><div class="form-actions">${btn('修改预约资料','booking',true)}${signStep?'<button class="btn secondary" data-journey-back>上一步</button>':''}<button class="btn" id="submit-appointment" data-journey-next ${!agreed||!hasSignature?'disabled':''}>${signStep===0?'下一份协议':'下一步：信息核对'}</button></div></section>`;
}
function policyEditor(){const p=state.policies?.[policyType];const titles=policyType==='home'?AGREEMENT_NAMES:['入托或到店服务协议','入托或到店数据保密协议',AGREEMENT_NAMES[2]];return `<p>不同服务单独发布适用协议，历史已签版本不会改变。</p>${serviceTypeSelect('policy-type',policyType)}${policyType==='home'&&!p?'<p class="note">尚未发布，可载入文档候选协议后编辑。编辑框可以修改，核对并发布后家长才能签署。</p>':''}${policyType==='home'?'<button class="btn secondary" data-action="load-material-policies">载入文档中的两份候选协议</button>':''}<div id="policy-review-notes" class="note">请核对服务范围和拍摄条款，签署时未分配的教师显示待分配。到店协议请填写机构审核通过的到店文本。</div><form id="policy-form">${field('organization','服务机构名称','text',p?.organization||state.policy?.organization||'')}${field('contact','机构联系及隐私事务方式','text',p?.contact||state.policy?.contact||'')}${titles.map((t,i)=>`<div class="field"><label for="policy-${i}">${esc(t)} *</label><textarea id="policy-${i}" required minlength="30" maxlength="20000" rows="9">${esc(p?.documents[i]?.text||'')}</textarea></div>`).join('')}<label class="check"><input id="policy-reviewed" type="checkbox" required>本组文件已经机构核对，可以发布。</label><button class="btn">发布本类型正式协议</button></form>`;}
async function loadConsultations(id){const owner=state.user?.id;if(id){const r=await api('/consultations/'+id);if(state.user?.id!==owner)return;consultDetail=r.consultation;consultMessages=r.messages;consultMessageNext=r.next;}else{const r=await api('/consultations');if(state.user?.id!==owner)return;consultItems=r.items;consultNext=r.next;consultDetail=null;}}
function consultStatus(c){return {pending:'等待安排老师',active:'交流中',closed:'已结束'}[c.status];}
function consultationRows(rows){return rows.map(c=>`<article class="card consult-row"><div class="between"><span class="tag">${esc(c.category)}</span><span class="tiny">${consultStatus(c)}</span></div><h3>${esc(c.preview.slice(0,70))}</h3><p>${esc(c.parentName)} · ${esc(c.teacherName||'老师待分配')}</p><a class="textbtn" href="#/consultation/${c.id}">查看咨询 →</a></article>`).join('');}
function consultationPage(id){if(id&&consultDetail){const c=consultDetail;return head(c.category+'咨询','这里的交流仅向你和负责人员开放。','consultation')+`<div class="note between"><span>${consultStatus(c)} · ${esc(c.teacherName||'老师待分配')}</span>${c.status==='closed'?(c.user_id===state.user.id?'<button class="textbtn" data-consult-status="reopen">重新开启</button>':''):'<button class="textbtn" data-consult-status="close">结束咨询</button>'}</div>${state.user.role==='admin'?`<section class="card"><label for="consult-teacher">负责老师</label><select id="consult-teacher"><option value="">待分配</option>${state.teachers.filter(t=>t.active).map(t=>`<option value="${t.id}" ${t.id===c.teacher_id?'selected':''}>${esc(t.name)}</option>`).join('')}</select><button class="btn secondary" data-consult-assign>保存分配</button></section>`:''}<section class="card institution-section">${consultMessageNext?'<button class="textbtn" data-consult-older>查看更早消息</button>':''}<div id="consult-messages">${consultationMessages(consultMessages)}</div></section>${c.status!=='closed'?consultForm(false):'<p class="note">本次咨询已结束，记录仍可查看。</p>'}`;}
 return head('育儿路上，有人一起想办法','专业老师的私密图文答疑，陪你理解宝宝的需要。')+`<div class="note">育儿指导不替代医学诊断；宝宝出现紧急不适，请及时就医。</div>${!staff()?`<div class="content-tabs">${CONSULT_CATEGORIES.map(t=>`<button data-consult-new="${t}">${t}</button>`).join('')}</div>`:'<p>管理员负责分配，老师仅处理分配给自己的咨询。</p>'}<div id="consult-list">${consultItems.length?consultationRows(consultItems):empty('还没有咨询记录。')}</div>${consultNext?'<button class="btn secondary" data-consult-more>加载更多</button>':''}`;
}
function consultationMessages(rows){return rows.map(m=>`<article class="consult-message ${m.user_id===state.user.id?'own':''}"><div class="between"><b>${esc(m.name)} ${m.role==='parent'?'· 家长':m.role==='admin'?'· 管理员':'· 老师'}</b><time class="tiny">${fmt(m.created_at)}</time></div><p class="preserve">${esc(m.text)}</p><div class="consult-images">${m.images.map(url=>`<button data-consult-photo="${url}" aria-label="查看咨询图片"><img src="${url}" alt="咨询附件" loading="lazy"></button>`).join('')}</div></article>`).join('');}
function consultForm(isNew,category){return `<form id="consult-form" class="card institution-section" data-new="${isNew}" data-category="${esc(category||'')}"><label for="consult-text">${isNew?'想请教老师的问题':'补充问题或回复'}</label><textarea id="consult-text" required maxlength="2000" rows="4"></textarea><label for="consult-files">添加图片（最多4张，每张10MB以内）</label><input id="consult-files" type="file" accept="image/png,image/jpeg,image/webp" multiple><p class="tiny">图片只用于本次咨询，发送前可重新选择。</p><div role="alert" class="error"></div><button class="btn">${isNew?'提交咨询':'发送消息'}</button></form>`;}
function bindJourneys(){
 if($('#policy-type'))$('#policy-type').onchange=async e=>{policyType=e.target.value||'home';await render();};
 if($('#final-verified'))$('#final-verified').onchange=e=>$('[data-journey-submit]').disabled=!e.target.checked;
 const f=$('#consult-form');if(f){let request=null;handleForm('#consult-form',async()=>{const owner=state.user.id,files=[...$('#consult-files').files];if(files.length>4)throw new Error('最多4张图片');const message=$('#consult-text').value,images=[];for(const file of files)images.push(await sitePhoto(file));if(owner!==state.user?.id||!f.isConnected)return;const payload={text:message,images,...(f.dataset.new==='true'?{category:f.dataset.category}:{version:consultDetail.version})},fingerprint=JSON.stringify(payload);if(!request||request.fingerprint!==fingerprint)request={fingerprint,key:crypto.randomUUID()};const result=await api(f.dataset.new==='true'?'/consultations':'/consultations/'+consultDetail.id+'/messages','POST',{...payload,requestKey:request.key});if(owner!==state.user?.id)return;if($('#modal').open)$('#modal').close();go('consultation/'+result.id);await render();toast('已发送给本次咨询的负责人员');});}
}
async function serviceReview(id){const owner=state.user?.id,r=await api('/appointments/'+id+'/review');if(state.user?.id!==owner)return;if(r.review){modal('本次入户服务评价',`<p class="review-stars">${'★'.repeat(r.review.rating)}${'☆'.repeat(5-r.review.rating)}</p><p class="preserve">${esc(r.review.text||'未填写文字评价')}</p><p>${fmt(r.review.created_at)}</p>`);return;}if(!r.canReview){modal('服务评价',empty('家长尚未提交评价。'));return;}
 modal('这次入户服务，感觉怎么样？',`<form id="service-review-form"><p>评价仅你和机构相关人员可见，提交后保留原始内容。</p><fieldset class="rating"><legend>服务评分 *</legend>${[1,2,3,4,5].map(n=>`<label><input type="radio" name="rating" value="${n}" required>${n} 星</label>`).join('')}</fieldset><label for="review-text">想对老师说的话（选填）</label><textarea id="review-text" maxlength="1000" rows="4"></textarea><div class="error" role="alert"></div><button class="btn">提交评价</button></form>`);handleForm('#service-review-form',async f=>{await api('/appointments/'+id+'/review','POST',{rating:Number(new FormData(f).get('rating')),text:$('#review-text').value});await serviceReview(id);toast('谢谢你的反馈');});
}
document.addEventListener('click',async e=>{const b=e.target.closest('button');if(!b)return;const d=b.dataset;try{
 if(d.journeyNext!==undefined){if(!hasSignature||!agreed)return;signedSteps[signStep]={signature:signData,agreed:true};signStep++;await render();}
 else if(d.journeyBack!==undefined){signStep=Math.max(0,signStep-1);await render();}
 else if(d.journeyClear!==undefined){signData='';hasSignature=false;signedSteps[signStep]=null;bindSignature();$('.signature .hint').hidden=false;$('#submit-appointment').disabled=true;}
 else if(d.journeySubmit!==undefined){if(b.disabled||!$('#final-verified').checked||!signedSteps[0]||!signedSteps[1])return;b.disabled=true;try{requestKey??=crypto.randomUUID();await flushDraft();await api('/appointments','POST',{profile:state.draft,serviceType:state.draft.serviceType,address:state.draft.address,slotId:state.draft.slotId,policyId:bookingPolicy().id,agreed:true,signatures:signedSteps,requestKey});state.draft={};draftDirty=false;resetSignature();await refresh();go('success');}catch(error){if(error.status===409){resetSignature();await refresh();go('booking');}throw error;}finally{if(b.isConnected)b.disabled=false;}}
 else if(d.serviceReview)await serviceReview(d.serviceReview);
 else if(d.consultNew){modal('向老师提问 · '+esc(d.consultNew),consultForm(true,d.consultNew));bindJourneys();}
 else if(d.consultPhoto)modal('咨询图片',`<img src="${esc(d.consultPhoto)}" alt="咨询图片完整预览">`);
 else if(d.consultAssign!==undefined||d.consultStatus){b.disabled=true;try{await api('/consultations/'+consultDetail.id+'/'+(d.consultStatus?'status':'assign'),'POST',d.consultStatus?{version:consultDetail.version,action:d.consultStatus}:{version:consultDetail.version,teacherId:$('#consult-teacher').value});await render();}finally{if(b.isConnected)b.disabled=false;}}
 else if(d.consultMore!==undefined){b.disabled=true;const r=await api('/consultations?before='+encodeURIComponent(consultNext));consultItems.push(...r.items);consultNext=r.next;$('#consult-list').insertAdjacentHTML('beforeend',consultationRows(r.items));if(!consultNext)b.remove();else b.disabled=false;}
 else if(d.consultOlder!==undefined){b.disabled=true;const r=await api('/consultations/'+consultDetail.id+'?before='+encodeURIComponent(consultMessageNext));consultMessages.unshift(...r.messages);consultMessageNext=r.next;$('#consult-messages').insertAdjacentHTML('afterbegin',consultationMessages(r.messages));if(!consultMessageNext)b.remove();else b.disabled=false;}
 }catch(error){toast(error.message);}});
