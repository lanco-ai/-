let institution={revision:0,intro:'',team:[],environment:[],notices:[]},siteDraft=null,reviewPosts=[],reviewNext=null,reviewFilter='pending';
function clearModuleState(){siteDraft=null;reviewPosts=[];reviewNext=null;reviewFilter='pending';}
function institutionHome(){return head('近邻托育','家门口的科学托育服务')+`<section class="institution-welcome"><div><span class="tag">用心照护 · 温暖陪伴</span><h2>离家近一点，<br>陪伴多一点。</h2><p>了解我们的机构、老师与照护环境。</p>${btn('查看预约与在园动态','service')}</div><div class="institution-mark">${careIllustration()}<span>近邻 · 陪伴成长</span></div></section><section class="card institution-section"><h2>机构简介</h2><p class="preserve">${esc(institution.intro||'机构介绍正在完善，欢迎关注后续更新。')}</p></section><section class="institution-section"><h2>师资团队</h2><div class="grid3">${institution.team.map(t=>`<article class="card staff-card">${t.image?`<img src="${t.image}" alt="${esc(t.name)}">`:`<span class="iconbox">${icon('user')}</span>`}<h3>${esc(t.name)}</h3><span class="tag">${esc(t.title)}</span><p class="preserve">${esc(t.intro)}</p></article>`).join('')||'<div class="card">师资介绍更新中。</div>'}</div></section><section class="institution-section"><h2>托育环境与活动</h2><div class="grid3">${institution.environment.map((e,i)=>`<figure class="card environment-card"><button class="gallery-photo" data-site-view="${i}" aria-label="放大查看：${esc(e.caption)}"><img loading="lazy" src="${e.image}" alt="${esc(e.caption)}"></button><figcaption>${esc(e.caption)}</figcaption></figure>`).join('')||'<div class="card">环境照片更新中。</div>'}</div></section><section class="card institution-section"><h2>机构公告</h2>${institution.notices.map(n=>`<details class="activity-chapter"><summary>${esc(n.title)}</summary><p class="preserve">${esc(n.text)}</p></details>`).join('')||'<p>暂无新公告。</p>'}</section>`;}
const GAME_CARDS=[{title:'找找小玩具',desc:'跟着宝宝的目光，一起发现身边的物品。',image:'assets/resource-5-0.png',sections:[ARTICLES[3].sections[0],ARTICLES[3].sections[2]],source:3},{title:'大块积木分分类',desc:'在成人陪伴下认识颜色，边整理边交流。',image:'assets/resource-5-1.png',sections:[ARTICLES[3].sections[1],ARTICLES[3].sections[2]],source:3},{title:'一起翻翻绘本',desc:'指一指，说一说，让阅读成为亲子小游戏。',image:'assets/resource-2-0.png',sections:ARTICLES[6].sections,source:6}];
function knowledge(tab='articles'){
  const tabs=`<nav class="content-tabs" aria-label="育儿知识分类"><a href="#/knowledge/games" ${tab==='games'?'aria-current="page"':''}>居家游戏方案</a><a href="#/knowledge" ${tab!=='games'?'aria-current="page"':''}>科普文章</a></nav>`;
  if(tab!=='games')return articleListing().replace('<div class="grid3">',tabs+'<div class="grid3">');
  return head('在家，也能好好玩','图片搭配简单玩法，跟随宝宝的兴趣与当前能力。')+tabs+`<div class="grid3">${GAME_CARDS.map((g,i)=>`<article class="card article-card"><img class="game-cover" src="${g.image}" alt="${esc(g.title)}工具图示"><div class="article-content"><span class="tag">居家游戏 · 图文方案</span><h3>${g.title}</h3><p>${g.desc}</p><button class="textbtn" data-game="${i}">查看玩法 →</button></div></article>`).join('')}</div>${state.user.role==='admin'?`<section class="card institution-section"><h3>机构待审活动原稿</h3><p>原文中的具体训练要求尚待核对，不作为已发布的家长指导。</p>${MATERIAL_CONTENT.chapters.map(c=>`<button class="textbtn draft-activity" data-activity-review="${c.id}">${esc(c.title)}</button>`).join('')}</section>`:''}`;
}
function showGame(id){const g=GAME_CARDS[id];if(!g)return;const source=ARTICLES[g.source];modal(g.title,`<img class="game-cover" src="${g.image}" alt="活动工具图示"><p>${g.desc}</p>${g.sections.map(([h,p])=>`<section class="morning-item"><h3>${esc(h)}</h3><p>${esc(p)}</p></section>`).join('')}<p class="tiny">图为工具示意资料。<a href="${source.source}" target="_blank" rel="noopener noreferrer">内容参考：${source.sourceName}</a></p>`);}
function institutionEditor(){siteDraft=structuredClone(institution);return `<div class="note">填写真实机构资料，发布后在家长首页展示。仅上传可公开展示的机构或师资照片，不使用宝宝私有记录中的照片。</div><p><button type="button" class="btn secondary" data-site-source>载入补充资料</button><span class="tiny"> 填入文档简介、师资和公告，保留当前环境照片；点击发布后生效。</span></p><section class="note"><label>导入首页照片包<input type="file" accept="application/json,.json" data-site-gallery></label><p class="tiny">导入会替换编辑框中的环境照片，保留简介、师资和公告。核对后点击发布。</p><p id="gallery-progress" role="status"></p></section><form id="institution-form"><div class="field"><label for="site-intro">机构简介</label><textarea id="site-intro" maxlength="2000" rows="5" required>${esc(siteDraft.intro)}</textarea></div>${['team','environment','notices'].map((kind,i)=>`<section class="institution-section"><h3>${['师资团队','环境照片','机构公告'][i]}</h3><div id="site-${kind}">${siteRows(kind)}</div><button type="button" class="btn secondary" data-site-add="${kind}">添加${['老师介绍','环境照片','公告'][i]}</button></section>`).join('')}<div id="site-error" class="error" role="alert"></div><button type="button" class="btn" data-site-publish>发布机构资料</button></form>`;}
function siteInput(kind,i,key,label,multi=false){const value=siteDraft[kind][i][key]||'';return `<div class="field"><label>${label}${multi?`<textarea data-site-field="${kind}/${i}/${key}" maxlength="2000" rows="3">${esc(value)}</textarea>`:`<input data-site-field="${kind}/${i}/${key}" value="${esc(value)}" maxlength="100">`}</label></div>`;}
function siteRows(kind){return siteDraft[kind].map((row,i)=>`<div class="card site-row">${kind==='team'?siteInput(kind,i,'name','老师姓名')+siteInput(kind,i,'title','职务 / 资质介绍')+siteInput(kind,i,'intro','个人介绍',true):kind==='environment'?siteInput(kind,i,'caption','环境说明'):siteInput(kind,i,'title','公告标题')+siteInput(kind,i,'text','公告正文',true)}${kind!=='notices'?`<label>公开展示照片<input type="file" accept="image/png,image/jpeg,image/webp" data-site-image="${kind}/${i}"></label><div id="site-preview-${kind}-${i}">${row.image?`<img class="site-preview" src="${row.image}" alt="待发布机构照片">`:''}</div>`:''}<button type="button" class="textbtn" data-site-remove="${kind}/${i}">移除此项</button></div>`).join('');}
function collectSite(){siteDraft.intro=$('#site-intro').value;$$('[data-site-field]').forEach(el=>{const[k,i,f]=el.dataset.siteField.split('/');siteDraft[k][i][f]=el.value;});}
async function sitePhoto(file){if(!['image/png','image/jpeg','image/webp'].includes(file.type)||file.size>10*1024*1024)throw new Error('请选择不超过 10MB 的 PNG、JPEG 或 WebP 图片');const im=await createImageBitmap(file);try{const ratio=Math.min(1,1200/im.width,900/im.height),c=document.createElement('canvas');c.width=Math.max(1,Math.round(im.width*ratio));c.height=Math.max(1,Math.round(im.height*ratio));c.getContext('2d').drawImage(im,0,0,c.width,c.height);for(const q of [.8,.6,.4]){const b=await new Promise(resolve=>c.toBlob(resolve,'image/webp',q));if(b&&b.size<=500*1024)return await new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(r.result);r.onerror=reject;r.readAsDataURL(b);});}throw new Error('图片仍过大，请选择更小的图片');}finally{im.close();}}
function moderationPage(){return `<nav class="content-tabs" aria-label="审核筛选">${[['pending','待审核'],['approved','已通过'],['rejected','已退回 / 下架']].map(([v,t])=>`<button data-review-filter="${v}" ${reviewFilter===v?'aria-current="page"':''}>${t}</button>`).join('')}</nav><div id="review-list">${reviewRows(reviewPosts)}</div>${reviewNext?'<button class="btn secondary" data-review-more>加载更多</button>':''}`;}
function reviewRows(rows){return rows.map(p=>`<article class="card review-card"><div class="between"><b>${esc(p.name)}</b><span class="tiny">${fmt(p.time)}</span></div><p class="preserve">${esc(p.text)}</p><span class="tag">${esc(p.tag||'家长交流')}</span><label>审核说明（退回或下架必填）<textarea id="review-note-${p.id}" maxlength="300">${esc(p.reviewNote)}</textarea></label><div class="form-actions"><button class="btn secondary" data-review-id="${p.id}" data-review-status="rejected">${p.moderation==='approved'?'下架帖子':'退回'}</button><button class="btn" data-review-id="${p.id}" data-review-status="approved">通过审核</button></div></article>`).join('')||empty('此分类暂无帖子。');}
document.addEventListener('change',async e=>{if(!e.target.dataset.siteImage)return;const input=e.target,[k,i]=input.dataset.siteImage.split('/'),owner=state.user?.id;try{if(!input.files[0])return;const image=await sitePhoto(input.files[0]);if(state.user?.id!==owner||!input.isConnected)return;siteDraft[k][i].image=image;$('#site-preview-'+k+'-'+i).innerHTML=`<img class="site-preview" src="${image}" alt="待发布机构照片">`;}catch(error){toast(error.message);}});
document.addEventListener('click',async e=>{const b=e.target.closest('button');if(!b)return;const d=b.dataset;try{
  if(d.game!==undefined)showGame(Number(d.game));
  else if(d.siteView!==undefined){const photo=institution.environment[Number(d.siteView)];if(photo)modal(esc(photo.caption),`<img src="${photo.image}" alt="${esc(photo.caption)}">`);}
  else if(d.siteSource!==undefined){b.disabled=true;try{const owner=state.user?.id,source=await api('/institution/source');if(state.user?.id!==owner||!b.isConnected)return;collectSite();siteDraft={...siteDraft,intro:source.intro,team:source.team,notices:source.notices};$('#site-intro').value=siteDraft.intro;for(const k of ['team','notices'])$('#site-'+k).innerHTML=siteRows(k);toast('补充资料已载入，请核对后发布');}finally{b.disabled=false;}}
  else if(d.siteAdd){collectSite();const k=d.siteAdd;if(siteDraft[k].length>=(k==='notices'?10:8))throw new Error('已达到此分类的最大数量');siteDraft[k].push({});$('#site-'+k).innerHTML=siteRows(k);}
  else if(d.siteRemove){collectSite();const[k,i]=d.siteRemove.split('/');siteDraft[k].splice(Number(i),1);$('#site-'+k).innerHTML=siteRows(k);}
  else if(d.sitePublish!==undefined){b.disabled=true;try{collectSite();institution=await api('/institution','PUT',siteDraft);go('home');await render();toast('机构资料已发布');}finally{b.disabled=false;}}
  else if(d.reviewFilter){reviewFilter=d.reviewFilter;await render();}
  else if(d.reviewMore!==undefined){b.disabled=true;const r=await api('/staff/posts?status='+reviewFilter+'&before='+reviewNext);reviewPosts.push(...r.posts);reviewNext=r.next;$('#review-list').insertAdjacentHTML('beforeend',reviewRows(r.posts));if(!reviewNext)b.remove();else b.disabled=false;}
  else if(d.reviewId){b.disabled=true;try{const p=reviewPosts.find(p=>p.id===Number(d.reviewId));await api('/staff/posts/'+p.id+'/review','POST',{status:d.reviewStatus,note:$('#review-note-'+p.id).value,version:p.reviewVersion});await render();toast('帖子审核状态已更新');}finally{b.disabled=false;}}
}catch(error){if($('#site-error')&&d.sitePublish!==undefined)$('#site-error').textContent=error.message;else toast(error.message);}});
document.addEventListener('submit',e=>{if(e.target.id==='institution-form')e.preventDefault();});

async function decodeInstitutionGallery(file){
  if(file.size>16*1024*1024)throw new Error('照片包不能超过 16MB');
  const pack=JSON.parse(await file.text());
  if(pack?.format!=='jinlin-institution-gallery-v1'||!Array.isArray(pack.environment)||pack.environment.length<1||pack.environment.length>8)throw new Error('请选择有效的首页照片包，最多 8 张');
  const rows=[];
  for(const row of pack.environment){
    if(!row||typeof row.caption!=='string'||!row.caption.trim()||row.caption.length>100||typeof row.image!=='string')throw new Error('照片包的图片或说明不完整');
    const m=/^data:(image\/(?:jpeg|png|webp));base64,([A-Za-z0-9+/=]+)$/.exec(row.image);
    if(!m)throw new Error('照片包仅支持 JPEG、PNG 或 WebP 图片');
    const bytes=Uint8Array.from(atob(m[2]),c=>c.charCodeAt(0));
    rows.push({caption:row.caption.trim(),image:await sitePhoto(new File([bytes],'gallery',{type:m[1]}))});
  }
  return rows;
}
document.addEventListener('change',async e=>{
  const input=e.target;if(input.dataset.siteGallery===undefined||!input.files[0])return;
  const owner=state.user?.id,form=$('#institution-form'),controls=[input,...form.querySelectorAll('button,input,textarea')],disabled=controls.map(c=>c.disabled);
  controls.forEach(c=>c.disabled=true);$('#gallery-progress').textContent='正在处理照片，请稍候…';
  try{const rows=await decodeInstitutionGallery(input.files[0]);if(state.user?.id!==owner||!input.isConnected)return;
    collectSite();siteDraft.environment=rows;$('#site-environment').innerHTML=siteRows('environment');$('#gallery-progress').textContent=`已载入 ${rows.length} 张照片，核对后点击发布。`;
  }catch(error){if(input.isConnected)$('#gallery-progress').textContent='导入失败：'+error.message;}
  finally{controls.forEach((c,i)=>c.disabled=disabled[i]);input.value='';}
});
