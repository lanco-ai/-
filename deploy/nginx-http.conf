# Initial HTTP configuration, used only to obtain the first TLS certificate.
server {
    listen 80;
    server_name care.example.com;
    location /.well-known/acme-challenge/ { root /var/www/certbot; }
    location / { return 503 "HTTPS configuration is in progress.\n"; }
}
